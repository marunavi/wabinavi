# わびなび（wabinavi.jp）

神社仏閣の巡り方を提案するWEBサイト。公開中。
オーナー: SIMBA（プログラミング初心者・iPhoneのみで作業）。
コードはすべてClaudeが書き、SIMBAはGitHubへの反映と目視確認を担当。

## 絶対に守ること

1. **現在のデザインを変更しない。** 文字サイズ・色・余白を勝手に変えない
2. **一度に大量の既存コードを変更しない。** 追記か、小さな差し替えに留める
3. **今動いているものを壊さないことが最優先。**
   神社検索／神社詳細／記事詳細／AIおすすめルート／モバイル表示
4. **既存のAPIキーや認証情報を新しく露出させない。**
   HTMLに新規追加しない。別の場所にコピーして重複管理しない
5. **神社マスターの原本には一切書き込まない**（Google Drive側）
6. **イベント情報はAIの推測禁止。** 「毎年開催されている」「おそらく今年も」で
   掲載しない。最終URLは公式サイトのみ。現在表示中のイベントを空にしない
7. **寺社への許諾依頼の文面は SIMBA が作る。** Claudeは作らない
8. **確証のない情報・写真は出さない。** 出典が確認できないものは表示しないほうが正しい

## ファイル構成と役割

| ファイル | 大きさ | 役割 |
|---|---|---|
| `index.html` | 1.3MB | 本体（モバイル）。`SHRINES` `ARTICLES` `THEMES` `USER_POSTS` などのデータもここ |
| `concierge.js` | 887KB | 後付け機能の本体。index.htmlの見た目を上書きする層 |
| `routes.js` | 206KB | さらに後付け。**index.htmlより先に読まれる** |
| `mypage.js` | 22KB | マイページ |
| `pc.html` | 239KB | PC版。**未完成のため一般公開は停止中**。concierge.jsを読まない |
| `articles-data.json` | 19KB | 記事の一覧データ |
| `articles/神社/` `articles/寺院/` | | 記事の実体（.md と .html の両方） |

読み込み順: `index.html` → `routes.js` → `concierge.js`

`index.html` は1.3MBあり、SIMBAのiPhoneからはアップロードが現実的でない。
**修正はできるだけ `routes.js`（小さい）に追記する形で行う。**

## 記事の作り方（既存33本から実測した規則）

- **画像3枚**（ヒーロー1＋本文2）
- **本文1,800字前後**（1,200〜2,300）
- **見出し（##）4つ**。2〜4つ目はほぼ固定文言:
  ① 記事ごとの具体的な見出し ②ご利益と歴史の深掘り
  ③アクセス・参拝の実用情報 ④まとめ
- **タイトル30〜40字**、`【○○】` で始める
- **1本で2つの寺社**を取り上げる（「A社とB社。」の形）
- 強調は `<strong>` タグ（Markdownの `**` ではない）
- 画像の説明は**都道府県・市町村まで**書く
- `excerpt` は**本文リードの冒頭46字をそのまま**切る
- ファイル名: `articles/寺院/寺院記事NN_YYYYMMDD_短い識別名.md` と `.html` の両方
- 登録: `articles-data.json` に1件追加（id は神社=`a`＋連番／寺院=`t`＋連番）
  ＋ `concierge.js` の `var ART_FILE = {` の直後に1行追加

## 過去にハマった罠

- **CSSの詳細度**: クラス数が要素数に勝つ。勝てないときは
  `.theme-card.theme-card.theme-card` のようにクラスを繰り返す
- **チカチカ（点滅）**: 同じDOMを複数の処理が書き合うと起きる。
  ポーリング（setInterval）で直すと**画面に出てから直す**ので点滅が見える。
  **MutationObserver で「画面に出る前」に直すこと。** 所有者は必ず1つに絞る
- **`!important` はインライン `style=""` に勝つ**（インライン側に!importantが無ければ）
- **Google Places `findPlaceFromQuery` は locationBias が無いと世界で一番有名な同名を返す。**
  「愛宕神社」で京都が返り、埼玉指定でも千葉の鹿島神宮が出た事故がある。
  必ず県名・住所を付けるか locationBias を渡し、**返ってきた住所の都道府県を検証する**
- **locationBias は「お願い」であって「命令」ではない。** 60km を指定しても、
  全国的に有名な同名（京都の愛宕神社）に押し切られて再発した（2026-09-14）。
  **返ってきた座標を必ず検算し、離れていたら捨てること。**
  `routes.js` の `__wabiPlaceBias` が、名前から `SHRINE_COORDS`（108件）を引いて
  その社自身の8km を指定し、30km以上離れた結果は ZERO_RESULTS にしている。
  `findPlaceFromQuery` だけでなく `textSearch` も包む必要がある
- **Places `nearbySearch` の `type:'place_of_worship'` は神社・寺・教会を全部返す。**
  神社だけ選んでも寺が混ざる原因だった
- **Google Maps の `dir/?api=1` は transit + waypoints を受け付けない**（入力画面のまま）
- **GitHubのアップロードURLは `/upload/<ブランチ>/<パス>`。**
  `/upload/data` はブランチ`data`の意味になる。正しくは `/upload/main/data`

## 未解決・要対応

- 記事 `a04`「鎌倉・円覚寺横田南嶺管長に聞く」の本文が「準備中の記事です」のまま。
  **実在する人物名が入っているので取り下げを推奨。** SIMBAの返事待ち
- 記事 `a01`「40選」はタイトルに40選とあるが実際の項目が40個ない
- 神社マスターのURL汚染3件:
  鶴岡八幡宮（ドメイン失効し無関係サイトへ転送）→ `https://www.hachimangu.or.jp/`、
  晴明神社（中国語版を指す）→ ルートへ、寒川神社（英語版）→ ルートへ
- 管理画面のGitHubトークンは Fine-grained・リポジトリ限定・Contents:Read&Write・
  90日期限に絞ることを推奨（未対応）
- 記事の写真はすべてウィキメディア・コモンズ。クレジット表示は `routes.js` の
  `__wabiImgCredit` が閲覧時にコモンズへ問い合わせて出す方式（2026-09-06導入）。
  ただし**個別記事HTMLを直接URLで開いた場合は出ない**（62ファイル）

## 作業の進め方

- 変更したら必ず動作確認する。Playwrightが使える（iPhoneのUAで見ること。
  PCのUAだと `index.html` が `pc.html` へ転送する）
- SIMBAへの説明は**日本語**で、操作手順は**①②③の番号＋1手順1行＋画面上の位置**を書く
  （例: 「画面右上の『共有』アイコン（□に↑）をタップします」）
- 専門用語を避け、何が起きたかを普通の言葉で説明する
- できないことは「できない」と早く言う。推測で埋めない
